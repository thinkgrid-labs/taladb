import{_ as a,o as s,c as t,a2 as n}from"./chunks/framework.BFZItJIV.js";const u=JSON.parse('{"title":"Introduction","description":"Learn what TalaDB is, how it works, and why it was built — the embedded database for local-first JavaScript apps, powered by a Rust core that runs in the browser, Node.js, and React Native.","frontmatter":{"title":"Introduction","description":"Learn what TalaDB is, how it works, and why it was built — the embedded database for local-first JavaScript apps, powered by a Rust core that runs in the browser, Node.js, and React Native."},"headers":[],"relativePath":"introduction.md","filePath":"introduction.md"}'),i={name:"introduction.md"};function r(d,e,o,p,c,l){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h1><h2 id="what-is-taladb" tabindex="-1">What is TalaDB? <a class="header-anchor" href="#what-is-taladb" aria-label="Permalink to &quot;What is TalaDB?&quot;">​</a></h2><p>Most JavaScript apps require three separate tools to handle structured queries, vector similarity search, and offline-first storage — each with its own API, each requiring a server. TalaDB replaces all three with a single <strong>embedded database built in Rust</strong> that runs entirely on the user&#39;s device, with no network dependency and no cloud subscription.</p><p>Data is stored as schemaless JSON-like documents organised into named <strong>collections</strong>. Queries use a MongoDB-inspired filter DSL. Vector indexes sit alongside regular document fields: a single <code>findNearest</code> call can rank by embedding similarity while filtering by metadata, giving you the hybrid search pattern that cloud vector databases charge for, running entirely on-device.</p><p>As local AI inference becomes mainstream (transformers.js, ONNX Web, WebGPU), applications generate embeddings on the client and need somewhere to store and search them. TalaDB is that place.</p><p>The same Rust core powers every runtime:</p><table tabindex="0"><thead><tr><th>Runtime</th><th>Package</th><th>Mechanism</th></tr></thead><tbody><tr><td>Browser</td><td><code>@taladb/web</code></td><td><code>wasm-bindgen</code> + OPFS</td></tr><tr><td>Node.js</td><td><code>@taladb/node</code></td><td><code>napi-rs</code> native module</td></tr><tr><td>React Native</td><td><code>@taladb/react-native</code></td><td>JSI HostObject (C FFI)</td></tr></tbody></table><p>All three surfaces expose a single unified TypeScript API from the <code>taladb</code> package, so application code never needs to branch on platform.</p><h2 id="architecture-overview" tabindex="-1">Architecture overview <a class="header-anchor" href="#architecture-overview" aria-label="Permalink to &quot;Architecture overview&quot;">​</a></h2><p>TalaDB is built in three layers:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────────────────────────────────────────────────────────┐</span></span>
<span class="line"><span>│  Layer 3 — TypeScript / JavaScript API                        │</span></span>
<span class="line"><span>│  wasm-bindgen (browser) · napi-rs (Node.js) · JSI (RN)       │</span></span>
<span class="line"><span>└──────────────────────────────┬───────────────────────────────┘</span></span>
<span class="line"><span>                               │  postcard bytes</span></span>
<span class="line"><span>┌──────────────────────────────▼───────────────────────────────┐</span></span>
<span class="line"><span>│  Layer 2 — Document + Vector Engine  (taladb-core)            │</span></span>
<span class="line"><span>│  Document model · B-tree indexes · Vector indexes             │</span></span>
<span class="line"><span>│  Query planner/executor · FTS · Migrations · Live queries     │</span></span>
<span class="line"><span>└──────────────────────────────┬───────────────────────────────┘</span></span>
<span class="line"><span>                               │  raw key/value bytes</span></span>
<span class="line"><span>┌──────────────────────────────▼───────────────────────────────┐</span></span>
<span class="line"><span>│  Layer 1 — KV Storage Engine                                  │</span></span>
<span class="line"><span>│  redb (native / Node.js) · OPFS backend (browser)            │</span></span>
<span class="line"><span>└──────────────────────────────────────────────────────────────┘</span></span></code></pre></div><p><strong>Layer 1 — Storage.</strong> <a href="https://github.com/cberner/redb" target="_blank" rel="noreferrer">redb</a> is a pure-Rust, B-tree embedded key-value store. In the browser, TalaDB replaces it with a custom OPFS backend that uses <code>FileSystemSyncAccessHandle</code> inside a SharedWorker, giving durable on-device persistence without IndexedDB&#39;s overhead.</p><p><strong>Layer 2 — Document + vector engine.</strong> <code>taladb-core</code> sits above the storage layer and knows nothing about JavaScript bindings. It provides the document model, secondary index key encoding, vector index storage and similarity search, the filter/update AST, the query planner, full-text search, and schema migrations. Documents and vector entries live in separate redb tables (<code>docs::</code>, <code>idx::</code>, <code>vec::</code>) but are updated atomically in the same transaction.</p><p><strong>Layer 3 — Bindings.</strong> Thin platform-specific wrappers translate JavaScript values into the Rust types that <code>taladb-core</code> expects and route them through the storage layer.</p><h2 id="repository-structure" tabindex="-1">Repository structure <a class="header-anchor" href="#repository-structure" aria-label="Permalink to &quot;Repository structure&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>taladb/</span></span>
<span class="line"><span>├── Cargo.toml                      # Rust workspace</span></span>
<span class="line"><span>├── pnpm-workspace.yaml</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>├── packages/</span></span>
<span class="line"><span>│   ├── taladb-core/                # Pure Rust core — no JS bindings</span></span>
<span class="line"><span>│   │   └── src/</span></span>
<span class="line"><span>│   │       ├── document.rs         # Value enum, Document struct (ULID IDs)</span></span>
<span class="line"><span>│   │       ├── engine.rs           # StorageBackend trait + redb implementation</span></span>
<span class="line"><span>│   │       ├── index.rs            # Secondary index key encoding</span></span>
<span class="line"><span>│   │       ├── collection.rs       # CRUD + vector index operations</span></span>
<span class="line"><span>│   │       ├── vector.rs           # Vector index, similarity math, encoding</span></span>
<span class="line"><span>│   │       ├── migration.rs        # Schema versioning</span></span>
<span class="line"><span>│   │       ├── crypto.rs           # AES-GCM-256 encryption wrapper</span></span>
<span class="line"><span>│   │       ├── watch.rs            # Live query subscriptions</span></span>
<span class="line"><span>│   │       └── query/</span></span>
<span class="line"><span>│   │           ├── filter.rs       # Filter AST</span></span>
<span class="line"><span>│   │           ├── planner.rs      # Index selection</span></span>
<span class="line"><span>│   │           └── executor.rs     # Scan + post-filter</span></span>
<span class="line"><span>│   │</span></span>
<span class="line"><span>│   ├── @taladb/web/                # Browser (wasm-bindgen + OPFS)</span></span>
<span class="line"><span>│   ├── @taladb/node/                # Node.js (napi-rs native module)</span></span>
<span class="line"><span>│   ├── @taladb/react-native/        # React Native (JSI HostObject + C FFI)</span></span>
<span class="line"><span>│   └── taladb/                     # Unified TypeScript package</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>└── examples/</span></span>
<span class="line"><span>    ├── web-vite/                   # React + Vite demo</span></span>
<span class="line"><span>    ├── expo-app/                   # Expo React Native demo</span></span>
<span class="line"><span>    └── node-script/                # Node.js script demo</span></span></code></pre></div><h2 id="packages" tabindex="-1">Packages <a class="header-anchor" href="#packages" aria-label="Permalink to &quot;Packages&quot;">​</a></h2><p>TalaDB is published as four focused npm packages:</p><table tabindex="0"><thead><tr><th>Package</th><th>Purpose</th></tr></thead><tbody><tr><td><code>taladb</code></td><td>Unified TypeScript API — auto-detects the platform and delegates to the right backend</td></tr><tr><td><code>@taladb/web</code></td><td>Browser WASM + OPFS backend</td></tr><tr><td><code>@taladb/node</code></td><td>Node.js napi-rs native module</td></tr><tr><td><code>@taladb/react-native</code></td><td>React Native JSI TurboModule</td></tr></tbody></table><h3 id="which-packages-do-i-install" tabindex="-1">Which packages do I install? <a class="header-anchor" href="#which-packages-do-i-install" aria-label="Permalink to &quot;Which packages do I install?&quot;">​</a></h3><p><strong>Browser (Vite, Next.js, etc.)</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> taladb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @taladb/web</span></span></code></pre></div><p><strong>Node.js</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> taladb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @taladb/node</span></span></code></pre></div><p><strong>React Native — shared codebase with web or Node.js</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> taladb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @taladb/react-native</span></span></code></pre></div><p>Use <code>openDB</code> from <code>taladb</code> everywhere. It detects React Native automatically.</p><p><strong>React Native — standalone app (RN only, no shared code)</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @taladb/react-native</span></span></code></pre></div><p>Import directly from <code>@taladb/react-native</code>. Calls are synchronous via JSI — no <code>await</code> needed. See the <a href="/guide/react-native.html#standalone-installation">React Native guide</a> for details.</p><p>The <code>taladb</code> package lists the platform packages as <code>optionalDependencies</code>, which means npm/pnpm won&#39;t fail the install if one isn&#39;t present — but it won&#39;t install them automatically either. You must add whichever platform package you need alongside <code>taladb</code>.</p><h2 id="taladb-vs-recached" tabindex="-1">TalaDB vs Recached <a class="header-anchor" href="#taladb-vs-recached" aria-label="Permalink to &quot;TalaDB vs Recached&quot;">​</a></h2><p><a href="https://recached.dev" target="_blank" rel="noreferrer">Recached</a> is our sibling project at ThinkGrid Labs. They are deliberately complementary, not competing — the question is never &quot;which one,&quot; it&#39;s &quot;which layer.&quot;</p><table tabindex="0"><thead><tr><th></th><th>TalaDB</th><th>Recached</th></tr></thead><tbody><tr><td>What it is</td><td>Embedded <strong>database</strong> inside the app</td><td>Cache + <strong>sync fabric</strong> between backend and clients</td></tr><tr><td>Data model</td><td>Documents with MongoDB-like queries, indexes, ACID transactions</td><td>Keys — strings, collections, JSON</td></tr><tr><td>Superpower</td><td>On-device vector + hybrid search, rich queries</td><td>Multi-client sync: scoped auth, live fan-out, offline outbox, exactly-once delivery</td></tr><tr><td>Server</td><td>None — runs entirely on-device</td><td>The server is the product (Redis-compatible)</td></tr><tr><td>Truth model</td><td><strong>Device-local truth</strong></td><td><strong>Shared truth</strong> across users and devices</td></tr></tbody></table><p>The one-line rule: <strong>TalaDB is where one device&#39;s data lives; Recached is how many devices agree.</strong> Reach for TalaDB when you need queryable structured data and semantic search on-device. Reach for Recached when many users or devices need to see the same live state.</p><p>They meet where an app needs both — locally queryable data that also syncs across users. TalaDB&#39;s <a href="/roadmap.html"><code>SyncAdapter</code></a> interface is designed to plug into a sync backbone, and Recached is a natural one: TalaDB owns the on-device query and vector engine, Recached owns the cross-device agreement.</p><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>TalaDB is production-ready. The Rust core, browser WASM, Node.js bindings, and React Native JSI layer are fully functional, tested, and stable across all supported platforms.</p><p>Try the <a href="https://taladb-playground.vercel.app/" target="_blank" rel="noreferrer">web demo</a> to see TalaDB running in the browser with OPFS persistence and on-device semantic search, or the <a href="https://appetize.io/app/b_ugmjhjghdkgnjux4lzkepvsfma" target="_blank" rel="noreferrer">mobile demo</a> to see it running on React Native. Follow the <a href="https://github.com/thinkgrid-labs/taladb" target="_blank" rel="noreferrer">GitHub repository</a> for progress updates.</p>`,39)])])}const b=a(i,[["render",r]]);export{u as __pageData,b as default};
